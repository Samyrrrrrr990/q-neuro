# Figure manifest

The paper builder copies the 20 publication figures generated from registered experiment artifacts
into this directory. Run `make figures` to regenerate the source plots and `make paper` to refresh
the manuscript copies, DOCX, modular LaTeX, and PDF.

No figure in the manuscript is decorative or hand-populated with result values.
